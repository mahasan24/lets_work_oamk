# API server (Better Auth lives at /api/auth/*)
VITE_SERVER_URL=http://localhost:3000

# Web app URL — used for password reset redirect links
VITE_APP_URL=http://localhost:3001

# Cloudinary cloud name (public, used for direct uploads)
VITE_CLOUDINARY_CLOUD_NAME=
