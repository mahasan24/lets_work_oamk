/**
 * Legacy app chrome. Prefer MarketingHeader / dashboard shells for product UI.
 * Kept only for transitional pages (e.g. /success) until those are restyled.
 */
export { default } from "@/components/marketing/marketing-header";
